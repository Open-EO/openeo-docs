name: PR Preview

on:
  pull_request:
    branches:
      - main
      - staging

permissions:
  contents: write

jobs:
  preview:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
         submodules: true

      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Optional: install Python packages if needed
      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Render site
        run: quarto render

      - name: Build JupyterLite
        run: |
          pip install -r jupyterlite-requirements.txt
          mkdir -p jupyterlite/files/notebooks/RandomForest-ForestFire
          cp client_examples/openeo-community-examples/python/RandomForest-ForestFire/RandomForestModelTraining.ipynb jupyterlite/files/notebooks/RandomForest-ForestFire/
          jupyter lite build --lite-dir jupyterlite --output-dir _site/jupyterlite --base-url /openeo-docs/previews/pr-${{ github.event.pull_request.number }}/jupyterlite/

      # -------------------------
      # ARCHIVE to gh-pages
      # -------------------------
      - name: Archive production build
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./_site
          destination_dir: previews/pr-${{ github.event.pull_request.number }}
          keep_files: true